//maybe we don't need this level. Just have modes.
globalThis.TalkApp = class TalkApp {
    constructor({
                    name,
                    initial_mode="main_menu",
                    title, //in dialog
                    backgound_color = tan,
                    width, height, x, y
                }) {

    }

  static defined_apps = []
   //status_variable_names ???

}